import { create } from "@storybook/theming/create";

export default create({
  base: "light",
  brandTitle: "<%= appName %>",
  brandUrl: "https://github.com/au-re/<%= appName %>",
  brandImage: "",
});
